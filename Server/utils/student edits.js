import studentData from '../../Client/src/data.json' assert { type: 'json' };

function editStudent(student) {
	for (let i = 0; i < studentData.length; i++) {
		if (studentData[i].fullName == student) {
		}
	}
}
